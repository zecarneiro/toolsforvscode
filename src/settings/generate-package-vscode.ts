import { Settings } from "../settings";

export namespace SettingsGeneratePackageVscode {
    // CMD
    export const CMD_GENERATE_PACKAGE = Settings.EXTENSION_NAME + '.generatepackage';

    export const STATUS_BAR = 'Generate VSI';
}